# Node.js CRUD API

A REST API for CRUD operations using Node.js, Express, and MongoDB.

## Structure
- `server.js`: The Express server setup.
- `models/Student.js`: Mongoose schema and validation rules.
- `routes/studentRoutes.js`: API routes for creating, reading, updating, and deleting student records.
