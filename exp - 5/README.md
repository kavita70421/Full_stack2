# React Hooks Demo

A React project exploring state management and performance hooks.

## Structure
- `src/components/Child.jsx`: A simple child component.
- `src/components/Heavy.jsx`: Simulates heavy computations to show how `useMemo` works.
- `src/App.jsx`: Uses `useState` for state updates and `useCallback` to cache functions.
